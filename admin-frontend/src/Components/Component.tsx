import React from 'react'
import styled from 'styled-components'

function Component() {
    return <Root>React App</Root>
}

export default Component

const Root = styled.div`
    position: relative;
    margin: 30px auto;

    font-weight: 400;
    font-size: 30px;
`
